<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="windowActionModeOverlay">true</item>
        <item name="android:actionModeBackground">@color/white</item>
        <item name="android:actionModeCloseDrawable">@drawable/ic_arrow_back</item>
        <item name="android:actionModeSplitBackground">@color/colorPrimary</item>
        <item name="android:actionModeCloseButtonStyle">@color/colorPrimary</item>
        <!-- specify shared element transitions -->
        <item name="android:windowContentTransitions">true</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <item name="android:windowDrawsSystemBarBackgrounds">true</item>
    </style>

    <style name="InitialTheme" parent="AppTheme"/>

    <style name="SplashTheme" parent="AppTheme">
        <item name="android:background">@drawable/background_splash</item>
    </style>

    <style name="BlankTheme" parent="AppTheme">
        <item name="android:background">@drawable/background_blank</item>

    </style>

</resources>
